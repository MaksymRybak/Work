EntityFramework Core 1.0

Create Models
Create DbContext

We use right database package to connect to our db. DB provider.

Using migrations. 
We can use entitiframework (ef) to create migrations of our db. 
Create and update DB's schema. We use so cold ef Tools.

Seeding database.
Insert data.

Repository pattern.
We use repository classes with it's own interfaces.

Logging
We use microsoft extensions.


